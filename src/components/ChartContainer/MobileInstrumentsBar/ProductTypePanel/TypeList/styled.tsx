import styled from 'styled-components'

export const TypeListContainer = styled.div<any>`
	position: absolute;
	z-index: 41;
	border-radius: 4px;
	border: 1px solid ${(props) => props.colors.panelBorder};
`
